---
description: Handles voice interactions by converting speech to structured financial actions and returning concise responses.
---

Voice input → Speech-to-text → Detect intent → Extract amount and category → Validate data → Execute tool → Generate short response (max 2 sentences)